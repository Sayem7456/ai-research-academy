---
title: "Sample: Linear Algebra — Eigenvectors"
date: 2026-06-08
tags:
  - math
  - linear-algebra
---

<Callout type="info">This sample demonstrates math, code blocks, and custom callouts in MDX.</Callout>

Below is an inline equation $E = mc^2$ and a displayed equation:

$$
A v = \lambda v
$$

```ts
// Simple TypeScript example
function dot(a: number[], b: number[]) {
  return a.reduce((s, v, i) => s + v * b[i], 0)
}

export default dot
```

![Example matrix](https://via.placeholder.com/400x200.png?text=Matrix+Visual)
